// Browser (as a part of websites)
// JavaScript code can be included in any website. The code then executes inside the browser (i.e on the machine of the website visitor).


// on any computer (eg: server side code)
// Thanks to the Node.js or Deno. JavaScript code can executed outside of the browser too, the code then executes directly on the machine.

// on mobile devices (eg: via embedded websites)
// With extra technologies like React Native, you can build mobile apps based on JavaScript. The code then executes on mobile device.
